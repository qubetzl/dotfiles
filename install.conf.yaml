- defaults:
    link:
      create: true
      relink: true

- clean:
    ~/:
    ~/.vim:
    ~/.ssh:

- link:
    ~/.bashrc:
      path: bashrc
      force: true
    ~/.profile:
      path: profile
      force: true
    ~/.vim/pack: vim/pack
    ~/.ssh/config: ssh/config
